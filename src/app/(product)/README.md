# Product surface

Cole aqui a UI do Lovable / vibecode (`import-external.mjs` já manda pra cá).

O `layout.tsx` (quando gerado) envolve tudo com `<RequireAccess />`.

Não coloque auth/billing custom — use `@/factory`.

